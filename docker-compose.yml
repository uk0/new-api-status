services:
  new-api-status:
    build: .
    container_name: new-api-status
    restart: unless-stopped
    ports:
      - "8787:8787"
    env_file: .env
    networks:
      - newapi-net

networks:
  newapi-net:
    external: true
    name: new-api_new-api-network
